import ConvertSDK
import SwiftUI

// Placeholder body — DEMO-2 installs the real SDK init
@main
struct ConvertSDKDemoApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Convert SDK Demo")
        }
    }
}
