# Contributing to ConvertSDK

Full contribution guidelines are forthcoming.

Before opening a PR, please run the following from the repo root and ensure each passes cleanly:

- `swift build`
- `swift test`
- `swiftlint lint --strict`
