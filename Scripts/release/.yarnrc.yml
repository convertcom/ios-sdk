nodeLinker: node-modules

# Supply-chain minimum release age: refuse to install any dependency version
# published less than 3 days ago (smash-and-grab malware is usually yanked
# within hours). Matches the gate in convertcom/javascript-sdk, convertcom/php-sdk
# and convertcom/python-sdk. No npmPreapprovedPackages entry here: this manifest
# has no internal @convertcom/* npm dependencies, so nothing in our own release
# chain can stall on a freshly-published package.
npmMinimalAgeGate: 4320
