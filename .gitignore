.build/
DerivedData/
*.xcodeproj/project.xcworkspace/xcuserdata/
.DS_Store
.swiftpm/
# Library package with zero external deps — Package.resolved is not pinned.
# Remove this line if the team later decides to pin external dev/test dependencies.
Package.resolved

# Isolated dev/CI tool for generating hash-parity-vectors.json (NFR18).
# Never part of the Swift package graph; installed deps must never be committed.
Scripts/generate-parity-vectors/node_modules/
